#include <bits/stdc++.h>
using namespace std;



// Driver code
int main()
{
     char val1 = 's';
   char val2 = '*';

   if(isalpha(val1))
   printf("The character is an alphabet\n");
   else
   printf("The character is not an alphabet\n");

   if(isalpha(val2))
   printf("The character is an alphabet\n");
   else
   printf("The character is not an alphabet");
   
	return 0;
}