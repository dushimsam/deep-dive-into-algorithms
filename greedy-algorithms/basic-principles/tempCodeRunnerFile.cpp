  // for (int i = 0;i < input.length();i++) {
  //   if (input[i] == snake[curr + 1] && map[snake[curr]]->first > 0) {
  //     minE = min(map[snake[curr]], minE);
  //     curr++;
  //     map[snake[curr]] += 1;
  //   }
  //   else if (i == snake[curr]) {
  //     if (map[curr] < minE)
  //       map[snake[curr]] += 1;
  //   }
  // }
