#include <bits/stdc++.h>
using namespace std;
typedef vector<int> vi;


int main() {
    set<int> arr;
    arr.insert(3);
    arr.insert(4);
    
    cout<<arr.find(3);
    
    return 0;
}