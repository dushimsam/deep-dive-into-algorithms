
let arr =[3,1,6,7,8,9,0];
let finalArr=[]
//Find the number of elmenents  less or equal to each element from the same array.


const mergeCount=(arr,temp,left,right)=>{

    if(right > left){
        let mid = (left + right)/2;
        
    }
}
// Divide the array into two parts. 
let left=0;
let right = arr.length-1;

let i = left; k=left; j=mid+1;
// Count

while((i<=mid) && (j<=right))
{
    if(arr[i] >= arr[j]){
      finalArr[i].value +=1; 
    }
}

//Copy the remaining arrays

while(())