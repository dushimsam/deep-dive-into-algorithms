#include<bits/stdc++.h>
using namespace std;

/***

             DISJOING PATH PROBLEM
Defition: What is the number of the maximum unique routes from source to the sink.
unique routes mean , each edge in graph can't exist in more than one route.

Description:
      This is a variation of the max-flow problem where each edge has weight of 1.

               APPROACH
for(edges of start_node)
   while(cpath.empty == false)
       int v = cpath.top()

       if(v == 0)
          

***/

int main(){
    return 0;
}